# 12: End-to-end smoke test and CI hardening

**What to build:** One browser test proves the whole path works: log in as the nurse, open a hero resident, ask the assistant a fixed question, and see an answer with a source chip. Continuous integration always runs lint, typecheck, and the unit suite, and runs the database-backed integration tests when connection secrets are present. The policy tests cover every table that holds resident data.

**Blocked by:** 08 (Simulator with simulated staff and a shift rhythm), 11 (Assistant: full tool set, source chips, saved threads, and AI audit)

**Status:** ready-for-human

- [x] A Playwright smoke test logs in as the nurse, opens a hero resident, asks a fixed question, and asserts an answer containing at least one source chip; it is the only test that calls the model
- [x] Database-backed tests skip cleanly with a visible reason when the connection variables are absent, so the unit suite always runs
- [x] GitHub Actions runs lint, typecheck, and the unit suite on every push, and runs the integration and smoke suites on the main branch when secrets are configured
- [x] Policy tests enumerate every table holding resident data and assert scoped reads for all three users and a rejected out-of-scope write for the nurse
- [x] The reset-and-reseed command is run at the start of the integration suite so tests never depend on leftover state, and the simulator is not running during tests
- [ ] The full suite passes locally against the hosted project and CI is green

## Comments

**2026-09-17, agent.** Built and checked locally; nothing pushed yet, so the last bullet stays open until the next push runs CI.

- Smoke test: `e2e/assistant-smoke.spec.ts` with `playwright.config.ts` (`@playwright/test` 1.63, Chromium only), run by `pnpm test:e2e`. The config builds the app and serves it with `next start` on port 3010 (`E2E_PORT` changes it), because `next dev` refuses to start beside a development server in the same directory. The test clicks the Meadows nurse's one-click sign-in, waits for "Welcome, Maria", opens Harold Doe, opens the drawer from the sidebar, asks "When was his last podiatry assessment?", and waits up to 90 s for a chip in `ul[aria-label="Sources"]` whose link goes to Harold's page; chips only render once the answer is complete, and the test also checks no error alert is showing. When done it removes the thread, its messages, and their access events with the service role. Without the hosted project or `ANTHROPIC_API_KEY` it skips with the missing names and starts no server. Passed locally: 9.9 s for the test, 28 s with the build. (The server logs one "destination stream closed early" when the test leaves the dashboard while it is still streaming; harmless.)
- Skipping with a reason: `src/test/env.ts` decides what is missing (`missingHostedVariables({ secretKey, anthropic })`, a placeholder URL counts as missing) and `src/test/hosted-project.ts` wraps it as `describeHosted(name, needs, factory)`; all ten integration suites use it, a skipped suite's name ends in "(skipped: NEXT_PUBLIC_SUPABASE_URL not set; copy .env.example to .env.local ...)", and the test setup prints the same line once at the top of the run. Checked by running with a placeholder URL.
- Test setup (`src/test/global-setup.ts`, vitest `globalSetup`): refuses to run while the simulator is running; compares the database with the last `seed_runs` row (every table's count, plus audit events, threads, and messages, which the seed leaves empty) and runs `pnpm db:seed` when anything differs (`CAREDESK_RESEED=always` or `never` override it); then subscribes to `audit_events` as the admin and writes and removes one note until Realtime announces it, trying a fresh subscription every 10 to 20 s for up to two minutes. In this session's run: reseed 22 s (it found the 3 threads and 11 events from the ticket 11 check), Realtime streaming on the second subscription at 17 s. That last step also fixes a pre-existing flake: before this change, the dashboard's Realtime test failed on a cold project (heard nothing in 8 s) and passed when run again.
- Simulator lock: `pnpm simulate` writes its process id to `.caredesk-simulator.lock` (gitignored) until it exits (`src/lib/simulator/lock.ts`, unit-tested); a second simulator refuses to start, the test setup refuses to run, and a lock left by a dead process is cleared on sight.
- Policy tests (`src/lib/scope/policies.integration.test.ts`): `RESIDENT_DATA_TABLES` is residents, the thirteen clinical tables, `audit_events`, and `assistant_messages`; `OTHER_RELATIONS` is everything else the API exposes. Leaving a table out of both lists fails to compile against the hand-maintained types, and one test compares the two lists with the live schema from PostgREST's OpenAPI description (secret key only, so the suite now needs it). For each account the row count of every resident-data table equals the seed's scoped count (events and messages against a service-role snapshot and the rule: a resident's event in the resident's scope, an event about nobody for the actor and admins, a message for its thread owner). The Meadows nurse's insert of a Harbor resident's row is rejected with 42501 on every one of the sixteen tables (a seeded Harbor row under a new id, hand-made rows for events and messages), and an anonymous client counts zero everywhere.
- CI (`.github/workflows/ci.yml`): `check` (lint, format, typecheck, unit tests with placeholders, build) on every push and pull request; `hosted-project` (main pushes only) reports whether the four secrets are set and posts a notice when not; `integration` (`pnpm test`, which reseeds first) and then `smoke` (Playwright with Chromium, report uploaded on failure) run on main when they are, under one concurrency group so two pushes never share the database. Secrets to add on the repository: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `ANTHROPIC_API_KEY`. Each push to main will then cost one model call.
- `pnpm check` green locally: 36 files, 257 tests, 151 s including the reseed and the Realtime wait. Docs: README "Tests" section and scripts table, `docs/database.md` under Seeding and Simulator.
- Left on the hosted project: a clean seed, no threads or events. Note that `pnpm test` now reseeds whenever anything has been written since the last seed, so threads made in the drawer for a demo do not survive a test run unless `CAREDESK_RESEED=never` is set.
